//! FamilyCom TUI — terminal user interface for chatting with peers.
//!
//! This binary connects to the local `familycomd` daemon via a Unix socket
//! and provides an interactive terminal interface for:
//! - Viewing discovered peers on the network
//! - Reading message history
//! - Composing and sending messages

fn main() {
    println!("familycom — FamilyCom TUI client (placeholder)");
}

//! SQLite database layer.
//!
//! Handles all persistence: messages, known peers, and local configuration.
//! Uses rusqlite with bundled SQLite so no system dependency is needed.

// Placeholder — full implementation in Phase 2
